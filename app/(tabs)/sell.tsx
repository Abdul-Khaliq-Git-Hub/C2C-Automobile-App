import { Link } from "expo-router";
import { View, Text } from "react-native";

const Sell = () => {
  return (
    <View>
      <Link href="/sign-in">Sign-in</Link>
    </View>
  );
};

export default Sell;
